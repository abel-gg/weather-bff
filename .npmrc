# The lockfile has to resolve identically on any machine, so the resolution
# algorithm is pinned here instead of being inherited from whatever happens to
# be in a developer's ~/.npmrc. A `legacy-peer-deps=true` in a user profile
# silently produces a different dependency tree than CI builds.
legacy-peer-deps=false
